# Optional. Without this key, Sidekick uses its built-in explainable demo advisor.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-latest

# Frontend dev server uses this by default.
VITE_API_URL=http://localhost:8000

